import { ToHideDirective } from './to-hide.directive';

describe('ToHideDirective', () => {
  it('should create an instance', () => {
    const directive = new ToHideDirective();
    expect(directive).toBeTruthy();
  });
});
