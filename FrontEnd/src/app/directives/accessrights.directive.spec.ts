import { AccessrightsDirective } from './accessrights.directive';

describe('AccessrightsDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessrightsDirective();
    expect(directive).toBeTruthy();
  });
});
