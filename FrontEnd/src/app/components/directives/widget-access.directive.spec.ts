import { WidgetAccessDirective } from './widget-access.directive';

describe('WidgetAccessDirective', () => {
  it('should create an instance', () => {
    const directive = new WidgetAccessDirective();
    expect(directive).toBeTruthy();
  });
});
